# MAGI P0 报告 — 三席容器化 QUINTE 首次端到端验证

日期：2026-07-31 · 执行：kimi(k3,设计态 + PA 回答者) · 仓库状态：MAGI `main@poc 后持续提交`,QUINTE `main@24ee25e`

## 判据复盘(P0 开工前写死)

| 判据 | 目标 | 实测 | 结论 |
| --- | --- | --- | --- |
| 机制 | 三席容器端到端完成率 ≥ 2/3 | **3/3 completed** | ✅ 达成 |
| 基线 | 测出同族残差重合代理量 ρ̂_w | mean pairwise overlap **0.6754** | ✅ 记录,待 P2 比 ρ̂_b |
| 成本 | 单 run 墙钟/token | 12–15 min/席;token 未采集(见 §6) | ⚠️ 部分达成 |

## 三席运行矩阵(成功代)

| 席 | run id | 墙钟 | result 残差 | dissent | 备注 |
| --- | --- | --- | --- | --- | --- |
| seat-m | `019fb6a1-3562-73d3-90e3-34c9fbb709d4` | 12m13s | 22(P0×6 HIGH×14 MED×2) | 16 | 第 4 代容器成功 |
| seat-d | `019fb6f3-e732-7533-9354-74b735187be9` | ~13min | 19 | — | 第 5 代成功(前四代死因见 §5) |
| seat-g | `019fb6e1-dd9b-7762-a638-0a2966c1ae4f` | 14m19s | 18(P0×5 HIGH×11 MED×2) | 12 | 第 2 代成功 |

三席同 brief(标定题:含 4 个预埋可靠性缺陷的缓存层设计评审),同 v1 policy(MiMo 族),
各自独立容器/QUINTE_HOME,PA 均由 kimi 回答(已声明污染 `pa_single_responder`)。

## 收敛测量(magi-merge,确定性合并)

- pairwise overlap(Dice):m|d **0.7317**、m|g **0.7000**、d|g **0.5946**;mean **0.6754**
- 聚类:**all=9 / pair=8 / unique=8**(25 个残差簇)
- **标定验证:5 个预埋缺陷全部进入 all-converged 集**——穿透(MAGI-004 P0)、fallback 级联
  (MAGI-002/005 P0)、写路径一致性(MAGI-001/003/006/009 HIGH)、可观测性(MAGI-007 HIGH)、
  TTL 惊群(MAGI-008 MEDIUM)。ground truth 全召回。
- unique 集中 7/8 是同义残差的**语言/措辞变体**(英文表述 vs 中文表述,CJK bigram shingle
  不跨语言),非真实分歧 → matcher 局限,记入 P1 改进项。

## ρ̂_w 基线解读

同族三席的残差重合代理 ~0.68。注意此数受 matcher 语言敏感性影响(中英文表述差异会压低),
真实语义重合更高。P2 族拆分后的 ρ̂_b 必须用**同一 matcher 同一 brief** 对比才有意义。
判据不变:ρ̂_b 显著 < ρ̂_w 则嵌套成立;降不动即任务侧 aleatoric，停止加机器。

## 故障链(P0 过程中定位并修复,全部已提交)

| # | 问题 | 根因 | 修复 | commit |
| --- | --- | --- | --- | --- |
| 1 | run 秒挂 "worker is not alive" | 镜像缺 procps,QUINTE 存活检查 `ps -p` 失败 | apt 装 procps | MAGI `616e961` 系 |
| 2 | codewhale EACCES | 首次运行下载二进制到 /usr/local,node 用户无权限 | 构建期 root 预热 | 同上 |
| 3 | cc 仲裁凭证缺失 | 宿主走 macOS Keychain,容器无 | `ANTHROPIC_API_KEY` env 注入(.env 600,gitignored) | 同上 |
| 4 | omp/opencode/claude 装坏 | `--ignore-scripts` 挡了 postinstall | 分包装法 + bun 独立层 | 同上 |
| 5 | codewhale provider 解析错 | npm latest 0.9.2 与宿主 0.8.66 行为漂移 | 钉版本 codewhale@0.8.66 / opencode@1.18.5 | 同上 |
| 6 | **QUINTE finalize 卡死** | **0.1.8 warrant/qualifier serde default 重序列化写出 null,下游严格 schema 打回(kilo R2 实测,报 null×4)** | **QUINTE `24ee25e`:skip_serializing_if + intake 归一化 + 2 回归测试(全测 88+ 绿)** | **QUINTE `24ee25e`** |
| 7 | seat-g claude 无法启动 | 其镜像 postinstall flake,二进制缺失 | Dockerfile 构建期 CLI 自检门(带病不出厂) | MAGI |

另两条**协议设计内行为**(非 bug):kilo R2 第三代自造 `auxiliary_assumption_for_challenge`
字段 → closed schema 永久失败不重试(保留的设计决策,第五代重跑即过);resume 不接受
failed 终态(需整轮重跑)。

## 成本与未采集项

- 墙钟:单席 12–15 min(R1 五路并发 ~5min + R2 串行 ~6min + R3/PA ~1min)。
- **token/费用未采集**:entrypoint 未汇总 lane usage;P1 应在 magi-merge 里聚合各 lane
  invocation 的 token 计数(适配器已记录),折算席 G 0.52× 月预算。
- PA 人力:3 次 PA 桥,每次 ~3 min(kimi 设计态时间)。

## 已知污染(trial_manifest 声明)

1. `same_family_all_seats` — P0 三席全 MiMo 族,只测机制与 ρ̂_w;无跨族结论。
2. `pa_single_responder` — 三席 R3 PA 均由 kimi 回答;P1 改容器内 PA-bot(仲裁族交叉)。
3. `cc_same_family_as_lanes` — Counterpart Arbiter(claude CLI)为 MiMo 后端。
4. `shared_evidence_snapshot` — 三席读同一标定 brief。

## 产物

- MAGI trace:`poc/vol/artifacts/magi-trace.json`(本报告同目录归档一份)
- 三席 run 目录:`poc/vol/state/<seat>/runs/<run-id>/`(含 hash 链工件,**未入 git**)
- 本报告:`poc/reports/P0-2026-07-31.md`

## P1 建议(按优先级)

1. **matcher 跨语言**:magi-merge shingle 加拉丁词干/翻译归一,或残差先归一语言再比对;
   当前 unique 里 7/8 是假分歧。
2. **token 采集**:entrypoint/merge 聚合 lane usage,成本进 trial_manifest.cost。
3. **容器内 PA-bot**:仲裁族交叉绑定自动作答,消除 pa_single_responder。
4. **外审入容器**:quinte-audit 打不进镜像(PATH 缺失,本次三席 auto-audit 均 skip);
   脚本+python3 入镜像,绑定走 QUINTE_AUDITOR=omp。
5. **resume 增强**:failed 终态在"末相失败"场景支持从失败相重入(本轮被迫整轮重跑)。
6. egress 白名单代理(P1 硬化)。

## P2 前置(QUINTE policy v2,另案)

route-audit.md §P2 六条:per-route family/model、仲裁路由可绑、audit_binding、manifest 记
`seat.family`、PROTOCOL/CLI 条文、回归测试。落地后换挂 v2-draft policies,同管线测 ρ̂_b。
